# 不确定命令字 — 待确认清单

> 以下命令字在 gg后台接口协议.md 中**没有找到明确对应**，或者**不确定是否需要**。
> 请逐条确认后，将确认的命令字补充到 cmd_config.yaml。

---

## 1. 🔴 加入集结（队员端）

**需求**：L1 指令 `JOIN_RALLY` — 队员派部队加入队长已创建的集结

**现状**：协议中只找到 `create_rally_war`（队长创建集结），但**没有找到显式的 "join_rally" 命令字**。

**猜测**：队员加入集结可能是通过 `dispatch_troop` 配合特定参数实现，例如：
- `march_type` 设为某个集结相关的值？
- `target_info` 中传入集结的 `unique_id`？
- 或者直接用 `march_type: 11`（增援）指向集结目标的坐标？

**影响**：这是团战核心机制，必须确认。

A: 加入集结使用 dispatch_troop 命令字，通过march_type参数来区分

---

## 2. 🔴 查询积分 / 活动数据

**需求**：感知层需要查询 — 双方积分、剩余时间、据点积分产出

**现状**：协议中找到的候选命令字：
- `get_svs_info_by_battlefield_id` — 但这是 SVS（跨服战）专用的，不确定是否适用于团战副本
- `op_get_event_score` — 这是勇士战令活动的积分领取，不是查询

**问题**：团战副本的积分数据通过什么命令字查询？还是积分数据包含在地图数据 (`get_map_brief_obj`) 的返回中？

A: 暂不处理积分计算，我们先把程序跑起来

---

## 3. 🟡 据点的 target_type 值

**需求**：攻击/驻防据点时，`dispatch_troop` 的 `target_type` 应该是什么？

**现状**：协议中 `target_type: 13` 对应"城寨"，看起来最可能。但团战副本中的据点是否等同于城寨？大据点和小据点的 target_type 是否相同？

**已在 cmd_config.yaml 中暂用 `target_type: 13`，待确认。**

---

## 4. 🟡 行军加速道具

**命令字**：`use_troop_speed_up_item`

**参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| unique_id | string | 行军队列的unique_id |
| item_id | int | 加速道具id |
| item_num | int | 道具数量 |
| cost_type | int | 0=道具, 1=宝石 |

**问题**：需求提到"行军加速道具，每次使用可减少固定百分比的行军时间"，但：
- 初期 AI 是否需要使用加速道具？还是作为后续优化？
- 具体的道具 id 是什么？
- 使用频率上限是多少？

---

## 5. 🟡 联盟建筑列表查询

**命令字**：`get_al_building_list`（无参数）

**问题**：据点信息是否已经包含在 `get_map_brief_obj` 的返回数据中？如果是，则不需要单独查询联盟建筑列表。

---

## 6. 🟢 地图搜索

**命令字**：`search_obj`

**参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| tar_type | int | 搜索类型（和map_obj类型一致）|
| tar_inside_type | int | 内置类型 |
| tar_lv | int | 目标等级 |

**问题**：是否需要主动搜索地图上的对象？还是通过 `get_map_brief_obj` 就能获取全图所有玩家和建筑的位置？如果后者已满足需求，则不需要此命令字。

---

## 7. 🟢 获取他人详细信息

**命令字**：`player_info_get`

**参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| uid | int | 对方的uid |

**问题**：需求说"无战争迷雾，可通过接口实时获取所有敌方信息"。但获取敌方详细信息（战力、兵种等）是否需要单独调用此接口？还是地图数据中已包含？

---

## 8. 🟢 解散集结

**命令字**：`rally_dismiss`

**参数**：
| 参数 | 类型 | 说明 |
|------|------|------|
| unique_id | string | 集结id |

**问题**：AI 是否需要主动解散已发起的集结？（如果战术变更需要取消集结）。目前判断非必需，但可能有用。

---

## 优先级说明

- 🔴 **必须确认**：缺少这些信息将无法实现核心功能
- 🟡 **建议确认**：影响重要功能，但可以先用猜测值开发
- 🟢 **可以暂缓**：可能不需要，或有替代方案
